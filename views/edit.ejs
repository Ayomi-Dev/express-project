<!DOCTYPE html>
<html lang="en">

    <%- include('../partials/head.ejs') %>

<body>
    <%- include('../partials/nav.ejs') %>
    <div class="page">
        <h1>Edit Your Personalised Verse</h1>
        <form action="/verses/<%= verse._id %>/edit" method="POST">
            <label for=""><%= verse.chapter %>:<%= verse.verse%></label>
            <input required type="text" name="chapter" value="<%= verse.chapter %>">
            <label for="">Verse</label>
            <input required type="number" name="verse" value="<%= verse.verse%>">
            <label for="">Personalise</label>
            <textarea required name="text"> <%= verse.text %> </textarea>
    
            <div class="action" style="display: flex; width: 100%;">
                <button type="submit">Save</button>
                <button style="background: crimson;"><a href="/verses/<%= verse._id %>" style="color: white;">Cancel</a></button>
            </div>
        </form>
    </div>
</body>
</html>